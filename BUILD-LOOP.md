# Cairn — Build Loop

Eight issues, flat and ordered. Work them in numerical order — each one assumes
the previous ones landed. There is no runbook beyond this file.

## The loop

1. **Set the effort** from the issue's `effort:` label. All eight tickets run on
   Opus 5; only the effort dial changes.
2. **Clear context** between issues. Every issue is written to be self-contained
   against the repo as the previous issue left it — carrying context forward
   costs more than it saves and lets stale assumptions leak between tickets.
3. **Paste the start prompt** for that issue from below.
4. **Verify by exercising the artifact** — run it, click it, drag it — against
   the issue's acceptance criteria. Not by reading the worker's summary of what
   it did. Then commit with `Closes #N`.

If a worker misses the same acceptance criterion twice, clear context and re-run
at the next effort step up rather than arguing with it in place.

---

## Start prompts

### Issue #1 — Foundations

> Read `PROJECT-SPEC.md` sections 3, 5, 7.3, and 8.2–8.4, then read GitHub issue
> #1 in full. Implement it exactly as specified: the Vite + React + Worker
> scaffold, `wrangler.toml` pointed at `tasks.charliepolito.com`, the D1 schema
> in `migrations/0001_init.sql` exactly as written in the issue, the design
> tokens as CSS variables wired into Tailwind v4, self-hosted Geist, and
> `shared/types.ts` plus `shared/order.ts` with unit tests. The architecture
> decisions in the issue are settled — implement them, do not redesign them.
> Build no UI beyond a placeholder render and no API routes beyond a 404 stub.
> Then walk the acceptance criteria one at a time, run each check, and report
> the actual result of each — including any that fail.

### Issue #2 — Auth & session system + login screen

> Read `PROJECT-SPEC.md` sections 6.1 and 9.1, plus 8.4 (buttons, inputs) and the
> press-feedback part of 8.5. Read `docs/PASSWORD-SETUP.md`. Then read GitHub
> issue #2 in full and implement it.
>
> This is the security boundary for the whole app and its failure modes are
> silent, so treat every acceptance criterion as something to execute rather than
> reason about. In particular: actually inspect the cookie flags in DevTools,
> actually confirm the session row is deleted from D1 on logout, and actually run
> six consecutive failed logins to see the limiter engage. Do not create a
> registration flow, a password reset, a default password, or a CSRF token — all
> are explicitly out of scope. Report each acceptance criterion with the result
> you observed.

### Issue #3 — Data & API layer

> Read `PROJECT-SPEC.md` sections 6.3, 6.4, 6.5, 6.7, 7.1, 7.2, and 7.3. Then
> read GitHub issue #3 in full and implement it: the seven endpoints in the
> issue's table (no others), boundary validation, and the two pure domain
> modules `shared/progress.ts` and `shared/upnext.ts` with tests.
>
> The endpoint list is complete and closed. Positions are generated by the
> client, never by the server. Write the domain tests first — the collapse of
> weighted progress into a plain count ratio when no difficulty is set, and the
> Up Next tie-break chain — then make them pass. Exercise the endpoints against
> `wrangler dev` and report what each check actually returned.

### Issue #4 — Client state, optimistic mutations, app shell and routing

> Read `PROJECT-SPEC.md` sections 6.2, 6.8, 7.3, 9.2, the segmented-control and
> toast parts of 8.4, and the press-feedback, thumb, and theme parts of 8.5 —
> skip 8.5's drag and completion sections entirely. Then read GitHub issue #4 in
> full and implement it.
>
> The store shape and the optimistic `mutate()` contract in the issue are fixed —
> every mutation goes through that one path with a capture, an apply, a request,
> and a revert. No component calls `fetch` directly. The failure cases are the
> point of this ticket: verify them by actually blocking the network in DevTools
> and watching the rollback and the retry, not by reading the code. Render
> placeholders for the four routes; screens are the next issue. Report each
> acceptance criterion with what you observed.

### Issue #5 — Screens

> Read `PROJECT-SPEC.md` sections 6.3–6.7, 7.1, 7.2, 8.3, 8.4 in full, and
> 9.3–9.8 in full. Read the press-feedback, stagger, and collapse paragraphs of
> 8.5, and skip its drag and completion-transition sections. Then read GitHub
> issue #5 in full and implement it.
>
> This is a large surface, all of it specified — build from the spec rather than
> inventing. Read through the store's selectors and write through `mutate()`;
> never sort a list in a component. Every list surface needs its empty, loading,
> and error state in this ticket, not later. No drag-and-drop and no completion
> animation — those are issue #6. No illustrations, no emoji, no decorative
> graphics anywhere. Walk the acceptance criteria in a browser and report the
> real result of each, including the 320px viewport check and the keyboard pass.

### Issue #6 — Drag-to-reorder and the completion transition

> Read `PROJECT-SPEC.md` section 8.5 in full — it is the governing document for
> this work — plus sections 2, 5 (the drag-and-drop constraint), 6.5, 6.6, and
> 7.3. Then read GitHub issue #6 in full and implement it.
>
> These are the two interactions the project is judged on. Every requirement in
> 8.5's drag and completion paragraphs is binding: grab-offset respect, pointer
> capture, 1:1 tracking, rubber-banding at the boundaries, edge auto-scroll,
> release-velocity handoff, mid-flight interruption, the refused drop on the
> Completed group, and one continuous movement on completion rather than a
> disappear-and-reappear. Transform and opacity only. Never lock input.
>
> No test can confirm this ticket. Verify it with your hands: a real iPhone in
> Safari and a desktop trackpad in Chrome. Report which criteria you physically
> exercised and on what device — if you could not test something on hardware, say
> so plainly rather than marking it passed.

### Issue #7 — Animation review pass

> Invoke the `review-animations` skill. Read `PROJECT-SPEC.md` sections 8.5 and
> 8.6, then read GitHub issue #7 in full.
>
> Review every piece of motion in the project — enumerate the files, do not
> sample. The standard defaults to flagging; approval is earned. Escalate on
> sight for the patterns listed in the issue. For each surviving animation,
> answer "why does this animate?"; an animation that cannot answer gets removed
> rather than tuned. Produce a `| Before | After | Why |` table covering every
> finding, post it as a comment on issue #7, and apply the fixes in the same
> change. Do not add animations the spec does not call for, and do not touch
> anything that is not motion code.

### Issue #8 — Full-output enforcement pass

> Invoke the `full-output-enforcement` skill. Read GitHub issue #8 in full.
>
> Walk every source file in `worker/`, `shared/`, `src/`, `migrations/`, and the
> root configs — every file, no sampling. Find and complete every placeholder,
> abridged file, and stub function left behind by the previous seven issues.
> Complete what is missing; change nothing that is present and working. No
> refactors, no renames, no restyling, no added tests beyond what a completed
> stub requires. If a stub cannot be finished without a design decision, stop and
> flag it in a comment on issue #8 instead of inventing behavior. Finish by
> running `tsc --noEmit`, the linter, the tests, and the build, and report the
> actual output of each.

---

## Standing rules for every ticket

- Build only what the spec specifies. No features, no abstractions for
  hypothetical futures, no error handling for cases that cannot occur.
- Validate at system boundaries — user input and external requests — and trust
  internal code.
- Report against what actually passed. If a check fails, say so with the output.
  If a step was skipped, say that.
- The "Decisions already made" table in `PROJECT-SPEC.md` §3 and the architecture
  block in each issue are settled. Implement them; do not relitigate them.
