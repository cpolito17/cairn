/**
 * Placeholder render. Screens arrive in later issues — this exists so the build
 * has something to show and so the tokens and the font can be verified.
 */
export function App() {
  return (
    <main className="flex min-h-dvh items-center justify-center px-gutter">
      <h1 className="text-hero text-text">cairn</h1>
    </main>
  );
}
